# Copy to keystore.properties and keep the real file private.
# Paths are resolved from the wear-app directory.
storeFile=../private-signing/codex-pet-watch-release.jks
storePassword=CHANGE_ME
keyAlias=codex-pet-watch
keyPassword=CHANGE_ME
