plugins {
    id "com.android.application"
}

android {
    namespace "com.codexwatch.pet"
    compileSdk 35

    defaultConfig {
        applicationId "com.codexwatch.pet"
        minSdk 30
        targetSdk 35
        versionCode 1
        versionName "0.1.0"
    }
}
